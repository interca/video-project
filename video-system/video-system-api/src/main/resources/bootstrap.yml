server:
  servlet:
    context-path: /system
  port: 63110
# 微服务配置
spring:
  application:
    #服务名称
    name: system-api
  datasource:
    url: jdbc:mysql://localhost:3306/video_system?useSSL=false
    username: root
    password: 13538026482
    driver-class-name: com.mysql.jdbc.Driver


# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml


#mybatiesplus配置
mybatis-plus:
  #mybaties xml 加载sql
  mapper-locations: classpath:mapper/*Mapper.xml
  configuration:
    # 驼峰
    map-underscore-to-camel-case: true
    # 自动配置日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# swagger 文档配置
swagger:
  title: "学成在线系统管理"
  description: "系统管理接口"
  base-package: com.video.system
  enabled: true
  version: 1.0.0
